# Auto-detect text files and normalize to LF in repo
* text=auto

# Force LF line endings for Python code
*.py text eol=lf

# Avoid merging raw data files
*.csv binary

# Normalize line endings for shell scripts
*.sh text eol=lf
